# アソビルド

YouTube「アソビルド」で、制限時間のなかで作ったミニゲーム／ツールを置いておく場所。

**作るものの規模は毎回同じ**。1プレイ30秒〜2分、一気に遊んで**15〜30分でひととおりやりきれる**もの。
制作時間（コーヒー1杯／ポテトM／30分／1日／7日）は企画ごとに変わるが、**規模は変えない**。
時間が余ったときに増やすのは要素ではなく密度。→ [docs/design/fun-doctrine.md](docs/design/fun-doctrine.md) §0

公開先: https://asobuild.coenoma.com ／ 運営: 合同会社コエノマ

---

## 🔴 このリポジトリは PUBLIC

「コードは1文字も書いていません、全部ここにあります」が企画の一部なので、**誰でも読める**。

- 秘密情報を一切置かない（APIキー・トークン・個人情報・未公開の企画メモ）
- 環境変数を必要とする機能を作らない。作りたくなったら設計が間違っている
- 撮影で映る前提で書く。コメントも人が読んで恥ずかしくないものにする

## 🔎 既存作を下敷きにするなら、先に調べる

「あの頃のあれを再現する」系の企画は、**作る前に調査を通す**。
流行った理由は企画ではなく作り込み（判定の甘さ・演出のタイミング・目標の刻み）にあるので、
そこを数値で特定してから作らないと「見た目は似ているが退屈」なものになる。

手順は [research-classic スキル](.claude/skills/research-classic/SKILL.md)、
ルールと法的な線引きは [docs/research/README.md](docs/research/README.md)。

調べて分かった工夫は [docs/design/feel-catalog.md](docs/design/feel-catalog.md) に貯める。
**調査するたびに次のゲームが強くなる**状態を保つのが狙い。

## 🎮 ゲームを1本作るときの流れ

```bash
npm run new -- <slug> --title "タイトル" --howto "あそびかた" --control tap --genre action
npm run dev                  # http://localhost:3020/g/<slug>
npm run fun -- <slug>        # 面白さゲート。落ちた項目の指示どおりに直す
```

0. **遊びの型を決める**（`action` / `puzzle` / `nurture` / `chance` / `oneshot`）
   型によって面白さゲートが見る項目が変わる。→ [docs/design/genre-map.md](docs/design/genre-map.md)
1. **雛形を出す**（手でファイルを作らない。登録簿への追記まで自動でやる）
2. **`step` と `draw` を書く**。ここだけがゲーム固有の仕事
3. **`npm run fun` が全部緑になるまで直す**
4. **自分で3回遊ぶ**。緑でも「もう一回」と思わなければ作り直す

作法の中身は [docs/design/fun-doctrine.md](docs/design/fun-doctrine.md)。
**新しいゲームを作る前に必ず読むこと。** ここに書いてある原則を毎回ゼロから考え直さない。

## ✋ ゲームを書くときの絶対ルール

| ルール | 理由 |
|---|---|
| `step` の中で `Math.random()` / `Date.now()` を使わない | 再現性が消えて面白さゲートが機能しなくなる。乱数は引数の `rng`、時間は `state.time` |
| `step` は引数を書き換えず、新しい state を返す | 同上 |
| `bot()` を必ず書く | 10行のボットが書けない＝ルールが一言で説明できない、というサイン |
| `meta.goals`（称号）を3〜5段階必ず置く | これが「次もやる理由」と「やりきった」を作る。無いと15分ももたない |
| `meta.genre` を実際の遊びに合わせる | 型を間違えるとゲートが見当違いの判定をする（育てるものに「放置すると死ぬ」を当ててしまう等） |
| リトライ画面・タイトル画面をゲーム側で作らない | 共通シェルが持っている。二重に作ると「1タップでもう一回」が壊れる |
| 色は `palette.ts` のキー名で指定する（生の `#hex` 禁止） | シリーズとしての見た目が揃わなくなる |
| 既存ゲームのコピーを作らない | 真似ていいのは遊びの仕組みまで（下記「線引き」） |
| 着想元があるなら `meta.inspiration` に書く | 何を借りて何を自作したかの記録が、公開してよいかの判断材料になる |
| **遊ばずに「完成」と言わない** | ゲートが全部緑でも面白いとは限らない。実際にそれで失敗した（下記） |

## ⚖️ 既存作を下敷きにするときの線引き

**遊びの仕組みを参考にすることと、具体的な表現をコピーすることは、まったく別のこと。**

- ⭕️ 参考にしてよい: ルールの組み合わせ（自動で前進する／1ボタンで跳ぶ／距離を競う／だんだん難しくなる）
- ❌ 必ずオリジナル: キャラクター・グラフィック・UI・BGM・SE・ステージ配置・タイトル/ロゴ・コード・その他アセット

公式の画像・音源・コードは、いかなる形でも持ち込まない。
**手元で試したものを、そのまま動画やWebに出してよいと考えない**（段階ごとに基準が上がる）。
Webで公開するものは「既存ゲームのコピー」ではなく
**「遊びの構造や当時のゲーム文化から着想したオリジナル作品」として成立**していること。

### 🔴 気づいたら必ず言う

実装しながら「これは公開版ではもっとオリジナルに離した方がよい」と判断したら、
**黙って作り続けず、その場で明示的に指摘すること**。
作れてしまうことと出してよいことは別で、後者を判断できるのは人間だけ。
指摘しないまま完成させると、判断する機会そのものが失われる。

線引きの詳細と公開前チェックリスト: [docs/design/rights-and-originality.md](docs/design/rights-and-originality.md)

## 📺 見せる前提で作る

**遊べれば成功ではない。** 開発実況で、作っている途中も完成後に遊ぶところも、
**遊んでいない視聴者が見ていて面白いか**を同じくらい重視する。

- **一目で状況が分かる** — うまくいってる／危ない／失敗した／記録を更新した、が画面だけで伝わる
- **成否がハッキリする指標を持つ** — スコア・距離・生存時間・コンボ・残機・自己ベスト・称号
- **失敗した瞬間が画面として分かりやすい** — 完璧に動くことより優先度が高い
- **すぐ遊べる** — 「では遊んでみます」から数秒でプレイが始まる
- **もう一回が一瞬** — 1プレイは短く、再挑戦は1タップ

ゲーム案を出すときは、面白さだけで選ばない。**権利面の注意点／動画で最も映える瞬間／
視聴者が結果を見たくなるポイント／成功と失敗の見えやすさ**も一緒に出す。

詳細: [docs/design/broadcast-design.md](docs/design/broadcast-design.md)
／ 撮るときの段取り: [recording スキル](.claude/skills/recording/SKILL.md)

## 🛑 ゲートが緑＝面白い、ではない

**一度これで失敗している。** たまごポン初版は面白さゲートを全項目通ったが、
遊んでみると「何をすればいいか画面から分からない」ゲームだった
（[経緯](docs/plans/002-tamago-remake/design.md)）。

問題は確認を怠ったこと自体ではなく、**怠ったことが誰にも分からなかった**こと。
そこで「人が遊んだ」を記録として残すようにした。

```bash
npm run dev                      # 実際に3回遊ぶ
npm run playtest -- <slug>       # 遊んだ結果を記録する（6つの問いに答える）
npm run ship                     # 公開前の確認（型・ゲート・ビルド＋遊んだ記録）
```

記録には**実装の指紋**が入る。ゲームを直すと記録は自動的に古くなり、`npm run ship` が落ちる。
つまり**直したら遊び直すまで出せない**。

問いは「面白かったか」ではなく、起きた事実を聞いている（説明を読まずに分かったか／
最初の10秒で失敗したか／なぜ失敗したか分かったか／気持ちよかったか／
指が勝手にもう一回押したか／音を消しても何が起きているか分かるか）。

### 指摘は消さずに貯める

遊んで出た指摘は、直して遊び直しても**履歴に残る**。
原則より効くのは「前に実際に言われたこと」なので、**新しく作る前に必ず読む**。

```bash
npm run status        # 「これまでに遊んで出た指摘」が出る
```

指摘を直したら、そのゲームを直して終わりにしない。
**一般化して [手触りカタログ](docs/design/feel-catalog.md) に書き、次に必ず読まれる場所に置く**。
やり方と実例: [docs/plans/004-hardening/loop-in-practice.md](docs/plans/004-hardening/loop-in-practice.md)

## 🤖 AIには確かめられないこと

次の3つは、**AIが「やった」「確認した」と言ってはいけない**。人に渡す。

- **実際に遊ぶ** — ブラウザ自動化では、タブが裏に回ると `requestAnimationFrame` が止まる。動きのあるものに触れない
- **見た目の良し悪しを決める** — 静止画は撮れるが、それは機能確認であって品質判断ではない
- **`playtest` の記録をつける** — 遊んでいないので答える資格がない

「実機で確認しました」と書けるのは、人が触ったときだけ。
AIは**「ここまでは確かめた／ここからは確かめていない」を必ず分けて**報告する。

## 👥 同じツリーを複数のセッションが触るとき

このリポジトリは、複数の Claude Code セッションが同時に作業することがある。

- **作業前に `git status`** を見る。自分が触っていない変更があれば、他が作業中とみなす
- **コミットは自分が作ったファイルだけ**。他セッションの成果物を巻き込まない
- **`cmux send` で他セッションに命令を送らない**（誤爆＝他人の作業への割り込み）
- 様子を知りたいだけなら `cmux read-screen --surface <ref>`（読むだけなので安全）
- 迷ったらコミットせずに残す。**置いてあること自体が引き継ぎになる**

## 🩺 いまの状態を見る

```bash
npm run status
```

やり残し（遊んで確かめていない・記録が古い・未コミット）と、
原則の目安に対するゲートの実際のしきい値を一望できる。
**食い違いに気づくためのもの**なので、迷ったらまずこれを叩く。

## 🧪 品質の担保

`npm run fun` は3種類のボットに数百回プレイさせて、次を数字で見る。
**見る項目は `meta.genre`（遊びの型）で切り替わる。**

- 放置すると死ぬか（育てる型では逆に「放置しても壊れないか」）／初見でも点が入るか／連打で勝てないか
- 腕前が効くか（運ゲーでないか）／上手くてもいつか終わるか／上振れがあるか
- **画面で何かが起きる間隔**（見ている人が退屈しないか）／一気に遊びきれる長さか

**落ちた項目には必ず直し方が出る。** 感想で直そうとせず、出た指示から手をつける。

基準を外す判断をしたときは `meta.funGate` で上書きし、**なぜ外すかをコメントに書く**。
黙って基準を下げない（下げた記録が残らないと、次から誰も基準を信じなくなる）。

## 📁 どこに何があるか

| 場所 | 中身 |
|---|---|
| `src/arcade/` | 共通ランタイム。ここを厚くするほど1本あたりが速くなる |
| `src/arcade/feel.ts` | 手触りの道具（先行入力・猶予・ヒットストップ・ゆれ・弾み） |
| `src/games/<slug>/` | ゲーム本体（`meta.ts` と `game.ts` の2つだけ） |
| `src/games/registry.ts` | 登録簿。`npm run new` が自動で追記する |
| `src/app/` | サイト（一覧とゲームページ） |
| `docs/design/` | 遊びの型の地図・面白さの原則・手触りカタログ・見せる前提の設計・権利の線引き。**作る前に読む** |
| `docs/research/` | 既存作の調査。ここに貯めた工夫がカタログと `feel.ts` に流れる |
| `docs/guides/` | 人間向け（はじめかた・収録の段取り） |

## 🚀 公開

`main` に push すると Vercel（Coenoma LLC チーム）が本番へ流す。
プレビューが要るときはブランチを切って PR を出す。

公開前に必ず: **`npm run ship`**
（`npm run check` の型・ゲート・ビルドに加えて、**人が遊んだ記録**があるかを見る）

## 🗣 言語

- 画面に出る文言・コメント・コミットメッセージ: 日本語
- 変数名・関数名・slug: 英語（半角英小文字とハイフン）
- 画面文言は「ひらがな多め・短く」（[retro-style.md](docs/design/retro-style.md) §5）

## ⏱ 制作時間が足りないとき／余ったとき

制作時間は企画ごとに変わる。**足りないとき**に削るのはこの順番。

1. 見た目の作り込み（形が分かれば十分）
2. 音（`sfx` の既定音で十分）
3. 演出（画面揺れ・点滅）

**削ってはいけないもの**: `bot()` / `reason()` / 難度の上昇 / 上振れ要素 / `meta.goals`。
「後で足す」と永久に足されないうえ、無いと面白さが成立しない。

**余ったとき**に足すのは規模ではなく密度。
ステージ・モード・要素を増やさない（15〜30分で遊びきれなくなる）。
判定の手触り、演出、称号の刻み、見た目の作り込みに使う。

## 🎬 収録との関係

制約（コーヒー1杯・ポテトM・カップ麺3分）は企画であって、品質基準ではない。
**時間切れで未完成のまま終わるのは動画として成功**だが、リポジトリに未完成のまま残さない。
撮影後に仕上げてから公開し、動画では「このあと直しました」と言う。

### カンペを流すのは、実装している側の仕事

**この番組は開発している画面そのものが本編。** 動くものを作るだけでは終わらない。

```bash
npm run say -- "ボットが全滅した。判定が厳しすぎるかも"
```

長い作業（ゲートを回す・ビルドする・調べる）に入る**前**に流す。
視聴者は画面しか見ていないので、**黙って考えている時間は「止まっている」と同じ**に見える。
撮っている人はカメラの前にいて手が空いていないので、ここは代われない。

`npm run new` / `fun` / `playtest` / `ship` は、打てば自動で流れる。
手で流すのは**そのあいだの考えごと**のほう。**うまくいっていない時ほど流す。**

> 実測（2026-08-12）: 自動で流れるゲート結果は66件貯まったのに、手で書いた一言は**2件**だけで、
> しかも全部が最初の2分間のものだった。別セッションはゲートを10回回して一言ゼロだった。
> 「気が向いたら流す」に頼ると流れない。

立ち上げから台本化までの段取り: **[recording スキル](.claude/skills/recording/SKILL.md)**
／ 収録の作法: [docs/guides/収録レギュレーション.md](docs/guides/収録レギュレーション.md)
